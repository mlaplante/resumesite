node_modules
*/.DS_Store
.DS_Store
npm-debug.log
tmp/
screenshots/
dist/
.wrangler/
blog-src/node_modules/
blog-src/.astro/

# Local secrets — .env for Astro PUBLIC_ vars, .dev.vars for `wrangler dev`.
# Only the example template is tracked.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# Local cache of LLM embeddings used by the auto-blog-draft dedupe pipeline.
scripts/.embeddings-cache.json
blog-src/**/CLAUDE.md
docs/**/CLAUDE.md

# Per-machine agent/tool state (the shared .claude/skills tree stays tracked).
.claude/settings.json
.entire/
.playwright-mcp/

# Screenshot PNGs (design reference only, not served)
blog-*.png
blog-article-*.png
blog-listing-*.png
blog-post-*.png
progress-bar-*.png
reading-progress-bar*.png
savvycal-*.png
.lighthouseci/
