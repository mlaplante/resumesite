---
import type { GetStaticPaths } from 'astro';
import { getCollection } from 'astro:content';
import BlogLayout from '../layouts/BlogLayout.astro';

export const getStaticPaths = (async ({ paginate }) => {
  const posts = (await getCollection('posts')).sort(
    (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
  );
  return paginate(posts, { pageSize: 10 });
}) satisfies GetStaticPaths;

const { page } = Astro.props;
const posts = page.data;
const categories = ['all', ...new Set(posts.map((p) => p.data.category))];
const isFirstPage = page.currentPage === 1;
---

<BlogLayout title="Blog" description="Thoughts on security, engineering, and building things.">
  <div class="listing-wrapper">
    <h1>Blog</h1>
    <p class="blog-subtitle">Thoughts on security, engineering, and building things.</p>

    <div class="search-box">
      <input type="text" id="blog-search" placeholder="Search posts..." autocomplete="off" />
    </div>

    <div class="category-filter">
      {categories.map((cat) => (
        <button class="filter-btn" data-category={cat}>
          {cat === 'all' ? 'All' : cat.replace(/-/g, ' ')}
        </button>
      ))}
    </div>

    {posts.length === 0 && <p class="no-posts">No posts yet. Check back soon.</p>}

    {isFirstPage && (
      <!-- Sponsored/Featured Content Slot -->
      <div class="featured-banner">
        <div class="featured-banner-content">
          <span class="featured-label">Services</span>
          <h3>Security Consulting & Architecture Reviews</h3>
          <p>15+ years of experience in enterprise security. I help teams build secure, scalable systems.</p>
          <a href="/#contact" class="featured-cta">Get in touch &rarr;</a>
        </div>
      </div>
    )}

    <div class="post-grid">
      {posts.map((post) => {
        const wordCount = post.body?.split(/\s+/).length ?? 0;
        const readTime = Math.max(1, Math.ceil(wordCount / 250));
        return (
          <a href={`${import.meta.env.BASE_URL}/${post.id}/`} class="post-card" data-category={post.data.category}>
            <div class="post-card-meta">
              <time datetime={post.data.date.toISOString()}>
                {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric', timeZone: 'UTC' })}
              </time>
              <span class="dot-sep">&middot;</span>
              <span>{readTime} min read</span>
            </div>
            <h2>{post.data.title}</h2>
            <p>{post.data.excerpt}</p>
            <span class="category-badge">{post.data.category.replace(/-/g, ' ')}</span>
          </a>
        );
      })}
    </div>

    {page.lastPage > 1 && (
      <nav class="pagination" aria-label="Blog pages">
        {page.url.prev ? (
          <a href={page.url.prev} class="pagination-link pagination-prev">&larr; Newer posts</a>
        ) : (
          <span class="pagination-link pagination-prev disabled">&larr; Newer posts</span>
        )}
        <span class="pagination-info">Page {page.currentPage} of {page.lastPage}</span>
        {page.url.next ? (
          <a href={page.url.next} class="pagination-link pagination-next">Older posts &rarr;</a>
        ) : (
          <span class="pagination-link pagination-next disabled">Older posts &rarr;</span>
        )}
      </nav>
    )}

    {isFirstPage && (
    <!-- Newsletter Signup -->
    <div class="newsletter-signup">
      <h3>Subscribe to the newsletter</h3>
      <p>New posts on security, AI, and engineering delivered to your inbox. No spam, unsubscribe anytime.</p>
      <form
        action="https://buttondown.com/api/emails/embed-subscribe/Mlaplante"
        method="post"
        class="newsletter-form"
      >
        <div class="newsletter-form-row">
          <input type="email" name="email" placeholder="you@example.com" required aria-label="Email address" />
          <button type="submit">Subscribe</button>
        </div>
      </form>
    </div>
    )}
  </div>
</BlogLayout>

<style>
  .listing-wrapper {
    max-width: 720px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .listing-wrapper {
      max-width: 900px;
    }
  }

  .blog-subtitle {
    font-family: 'Poppins', sans-serif;
    font-size: 16px;
    color: #6b7280;
    margin-bottom: 32px;
    font-weight: 400;
  }

  .search-box {
    margin-bottom: 20px;
  }

  #blog-search {
    width: 100%;
    padding: 12px 16px;
    font-family: 'Poppins', sans-serif;
    font-size: 15px;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    background: #fff;
    color: #1a1a1a;
    outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  #blog-search::placeholder {
    color: #9ca3af;
  }

  #blog-search:focus {
    border-color: #2980b9;
    box-shadow: 0 0 0 3px rgba(41, 128, 185, 0.1);
  }

  .category-filter {
    display: flex;
    gap: 8px;
    margin-bottom: 32px;
    flex-wrap: wrap;
  }

  .filter-btn {
    font-family: 'Poppins', sans-serif;
    font-size: 13px;
    font-weight: 500;
    padding: 6px 16px;
    border: 1px solid #e5e7eb;
    border-radius: 100px;
    background: transparent;
    cursor: pointer;
    color: #6b7280;
    transition: all 0.2s;
    text-transform: capitalize;
  }

  .filter-btn:hover {
    border-color: #2980b9;
    color: #2980b9;
  }

  .filter-btn.active {
    background: #2980b9;
    color: #fff;
    border-color: #2980b9;
  }

  .post-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
  }

  @media (min-width: 768px) {
    .post-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .post-card {
    display: flex;
    flex-direction: column;
    padding: 24px;
    background: #fff;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    text-decoration: none;
    transition: all 0.2s;
  }

  .post-card:hover {
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    border-color: #d1d5db;
    transform: translateY(-2px);
  }

  .post-card h2 {
    font-family: 'Poppins', sans-serif;
    font-size: 18px;
    font-weight: 700;
    color: #111827;
    margin: 8px 0;
    line-height: 1.35;
    letter-spacing: -0.2px;
  }

  .post-card p {
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
    color: #6b7280;
    margin: 0;
    line-height: 1.6;
    flex: 1;
  }

  .post-card-meta {
    display: flex;
    align-items: center;
    gap: 6px;
    font-family: 'Poppins', sans-serif;
    font-size: 13px;
    color: #9ca3af;
    font-weight: 400;
  }

  .dot-sep {
    color: #d1d5db;
  }

  .category-badge {
    display: inline-block;
    margin-top: 14px;
    font-family: 'Poppins', sans-serif;
    font-size: 12px;
    font-weight: 500;
    color: #2980b9;
    background: rgba(41, 128, 185, 0.08);
    padding: 4px 10px;
    border-radius: 100px;
    text-transform: capitalize;
    width: fit-content;
  }

  .no-posts {
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
    color: #9ca3af;
    text-align: center;
    padding: 60px 0;
    grid-column: 1 / -1;
  }

  /* Featured/Sponsored Banner */
  .featured-banner {
    margin-bottom: 32px;
    padding: 28px 32px;
    background: linear-gradient(135deg, rgba(41, 128, 185, 0.06) 0%, rgba(41, 128, 185, 0.02) 100%);
    border: 1px solid rgba(41, 128, 185, 0.15);
    border-radius: 12px;
  }

  .featured-label {
    font-family: 'Poppins', sans-serif;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #2980b9;
    background: rgba(41, 128, 185, 0.08);
    padding: 3px 10px;
    border-radius: 100px;
  }

  .featured-banner h3 {
    font-family: 'Poppins', sans-serif;
    font-size: 20px;
    font-weight: 700;
    color: #111827;
    margin: 12px 0 8px;
    line-height: 1.3;
  }

  .featured-banner p {
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
    color: #6b7280;
    margin: 0 0 16px;
    line-height: 1.6;
  }

  .featured-cta {
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
    font-weight: 600;
    color: #2980b9;
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .featured-cta:hover {
    opacity: 0.7;
  }

  /* Newsletter Signup */
  .newsletter-signup {
    margin-top: 48px;
    padding: 32px;
    background: linear-gradient(135deg, rgba(41, 128, 185, 0.06) 0%, rgba(41, 128, 185, 0.02) 100%);
    border: 1px solid rgba(41, 128, 185, 0.15);
    border-radius: 12px;
    text-align: center;
  }

  .newsletter-signup h3 {
    font-family: 'Poppins', sans-serif;
    font-size: 20px;
    font-weight: 700;
    color: #111827;
    margin: 0 0 8px;
  }

  .newsletter-signup p {
    font-family: 'Poppins', sans-serif;
    font-size: 15px;
    color: #6b7280;
    margin: 0 0 20px;
    line-height: 1.6;
  }

  .newsletter-form-row {
    display: flex;
    gap: 10px;
    max-width: 480px;
    margin: 0 auto;
  }

  @media (max-width: 480px) {
    .newsletter-form-row {
      flex-direction: column;
    }

    .newsletter-form-row button {
      width: 100%;
    }
  }

  .newsletter-form-row input[type="email"] {
    flex: 1;
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
    padding: 10px 16px;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    background: #fff;
    color: #1a1a1a;
    outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .newsletter-form-row input[type="email"]::placeholder {
    color: #9ca3af;
  }

  .newsletter-form-row input[type="email"]:focus {
    border-color: #2980b9;
    box-shadow: 0 0 0 3px rgba(41, 128, 185, 0.1);
  }

  .newsletter-form-row button {
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
    font-weight: 600;
    padding: 10px 24px;
    border: none;
    border-radius: 8px;
    background: #2980b9;
    color: #fff;
    cursor: pointer;
    transition: background 0.2s;
    white-space: nowrap;
  }

  .newsletter-form-row button:hover {
    background: #1a6da3;
  }

  /* Pagination */
  .pagination {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 40px;
    padding: 16px 0;
    border-top: 1px solid #e5e7eb;
    font-family: 'Poppins', sans-serif;
    font-size: 14px;
  }

  .pagination-link {
    font-weight: 500;
    color: #2980b9;
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .pagination-link:hover {
    opacity: 0.7;
  }

  .pagination-link.disabled {
    color: #d1d5db;
    pointer-events: none;
  }

  .pagination-info {
    color: #6b7280;
    font-size: 13px;
  }

  /* Dark mode */
  @media (prefers-color-scheme: dark) {
    #blog-search { border-color: #334155; color: #f1f5f9; background: #1e293b; }
    #blog-search::placeholder { color: #64748b; }
    #blog-search:focus { border-color: #1a5276; box-shadow: 0 0 0 3px rgba(26, 82, 118, 0.2); }
    .blog-subtitle { color: #94a3b8; }
    .filter-btn { border-color: #334155; color: #94a3b8; }
    .filter-btn:hover { border-color: #1a5276; color: #60a5fa; }
    .filter-btn.active { background: #1a5276; border-color: #1a5276; color: #fff; }
    .post-card { background: #1e293b; border-color: #334155; }
    .post-card:hover { box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3); border-color: #475569; }
    .post-card h2 { color: #f1f5f9; }
    .post-card p { color: #94a3b8; }
    .post-card-meta { color: #64748b; }
    .dot-sep { color: #475569; }
    .category-badge { color: #60a5fa; background: rgba(96, 165, 250, 0.1); }
    .no-posts { color: #64748b; }
    .featured-banner { background: linear-gradient(135deg, rgba(26, 82, 118, 0.15) 0%, rgba(26, 82, 118, 0.05) 100%); border-color: rgba(96, 165, 250, 0.15); }
    .featured-label { color: #60a5fa; background: rgba(96, 165, 250, 0.1); }
    .featured-banner h3 { color: #f1f5f9; }
    .featured-banner p { color: #94a3b8; }
    .featured-cta { color: #60a5fa; }
    .newsletter-signup {
      background: linear-gradient(135deg, rgba(26, 82, 118, 0.15) 0%, rgba(26, 82, 118, 0.05) 100%);
      border-color: rgba(96, 165, 250, 0.15);
    }
    .newsletter-signup h3 { color: #f1f5f9; }
    .newsletter-signup p { color: #94a3b8; }
    .newsletter-form-row input[type="email"] {
      background: #0f172a;
      border-color: #334155;
      color: #f1f5f9;
    }
    .newsletter-form-row input[type="email"]::placeholder { color: #64748b; }
    .newsletter-form-row input[type="email"]:focus {
      border-color: #1a5276;
      box-shadow: 0 0 0 3px rgba(26, 82, 118, 0.2);
    }
    .newsletter-form-row button { background: #1a5276; }
    .newsletter-form-row button:hover { background: #154360; }
    .pagination { border-top-color: #334155; }
    .pagination-link { color: #60a5fa; }
    .pagination-link.disabled { color: #475569; }
    .pagination-info { color: #94a3b8; }
  }
</style>

<script>
  // Category filter
  document.querySelectorAll('.filter-btn').forEach((btn) => {
    btn.addEventListener('click', () => {
      document.querySelectorAll('.filter-btn').forEach((b) => b.classList.remove('active'));
      btn.classList.add('active');
      applyFilters();
    });
  });
  document.querySelector('.filter-btn')?.classList.add('active');

  // Search filter
  const searchInput = document.getElementById('blog-search') as HTMLInputElement;
  searchInput?.addEventListener('input', () => applyFilters());

  function applyFilters() {
    const activeBtn = document.querySelector('.filter-btn.active') as HTMLElement;
    const cat = activeBtn?.getAttribute('data-category') || 'all';
    const query = searchInput?.value.toLowerCase().trim() || '';

    document.querySelectorAll('.post-card').forEach((card) => {
      const el = card as HTMLElement;
      const matchesCat = cat === 'all' || el.getAttribute('data-category') === cat;
      const text = el.textContent?.toLowerCase() || '';
      const matchesSearch = !query || text.includes(query);
      el.style.display = matchesCat && matchesSearch ? '' : 'none';
    });
  }
</script>
