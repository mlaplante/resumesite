# zizmor configuration — see .github/workflows/lint-workflows.yml.

rules:
  # This workflow pushes a branch back to the repo, so the checkout step
  # must keep its credentials. Every other workflow sets
  # `persist-credentials: false`.
  artipacked:
    ignore:
      - generate-blog-post.yml
